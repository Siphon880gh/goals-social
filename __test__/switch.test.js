describe('Test switching successful', () => {

    test('This test proves that you can run two separate tests with `npm run test` and `npm run test2`. Test2 requires you to drop database between test suites.', () => {
        expect(true).toBe(true);
    });

});